{
  "name": "firstproject-tests",
  "version": "1.0.0",
  "description": "Root package for Playwright tests",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:calculator": "playwright test tests/calculator.spec.ts",
    "test:api": "playwright test tests/api.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3"
  }
}
